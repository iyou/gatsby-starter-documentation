language: node_js

node_js:
  - "iojs"
  - "7"

branches:
  only:
  - master

before_script:
  - npm install

script:
  - gatsby build

deploy:
  provider: pages
  local_dir: public
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
